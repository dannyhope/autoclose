# Windows permission justification (CWS)

**windows** — Used only for the optional Tile all tabs action so the user can arrange remaining tabs after closing. It does not access other windows’ page content.
